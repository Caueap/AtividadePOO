package Questao1.explicacoes;

public class ExplicacaoAtributosEMetodos {

    /*Na orientação a objetos, um atributo é uma variável que serve para dar características
    a uma classe. Como sabemos, na orientação a objetos, tentamos representar o mundo real
    nos sistemas que criamos. Para isso, construimos códigos componentizados, em que várias
    partes do sistema se comunicam entre si. Nesse contexto surge o conceito de classe,
    que por sua vez levanta o conceito de atributo. Na programação estruturada, temos variáveis
    que servem para armazenar dados e fazer o código funcionar. Podemos dizer, então, que os atributos
    em orientação a objetos são o análogo às variáveis na programação estruturada. Atributos
    também podem ser chamados de variáveis, mas no contexto da orientação a objetos, o termo
    atributo adquire um significado a mais, pois se torna um componente de uma classe, que,
    por sua vez, é uma especificação para a criação de um objeto que representará um elemento
    do mundo real. Um atributo, portanto, pode ser definido como uma variável com um significado
    um pouco mais profundo, no que se refere à abstração do sistema.

    Já quando falamos de métodos, estes podem ser entendidos como pequenos trechos de código
    que realizam alguma funcionalidade. Podem também ser chamados de função. Nesse cenário
    de abstração que a orientação a objetos traz, um método pode ser entendido, algumas vezes,
    como o "comportamento" ou "ação" de uma classe. Alguns exemplos de atributos e métodos
    estão no pacote exemplo.
     */
}
